# Keeps the SHA-pinned actions in .github/workflows/ current (Dependabot
# updates the pinned commit and the trailing version comment together).
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
