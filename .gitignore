dist/
__pycache__/
.pytest_cache/
*.pyc
# Re-include: a global gitignore pattern like '*private*' would otherwise
# silently drop this CI validator from commits (it did once).
!scripts/ci/check_no_private_contact.py
